# تذكير سريع - النسخ الاحتياطي والاستعادة ⚡

## عمل نسخة احتياطية كاملة (5 دقائق)

### 1️⃣ نسخ الكود
```
1. افتح مجلد d:\تطبيقي
2. Ctrl + A (تحديد الكل)
3. Ctrl + C (نسخ)
4. اذهب لمجلد النسخ الاحتياطية
5. Ctrl + V (لصق)
```

### 2️⃣ نسخ قاعدة البيانات
```powershell
cd d:\تطبيقي
node backup-firestore.js
```

**✅ تم! النسخة محفوظة في:**
- الكود: في المجلد الذي أنشأته
- البيانات: في `d:\تطبيقي\backups\firestore-YYYY-MM-DD`

---

## استعادة التطبيق

### استعادة الكود
```
1. افتح النسخة الاحتياطية
2. Ctrl + A → Ctrl + C
3. اذهب لـ d:\تطبيقي
4. Ctrl + V
5. افتح Terminal واكتب:
```
```powershell
cd d:\تطبيقي
npm install
npm run dev
```

### استعادة قاعدة البيانات
```powershell
cd d:\تطبيقي
node restore-firestore.js backups/firestore-2025-12-12
```
*⚠️ غيّر التاريخ للنسخة المطلوبة*

---

## أوامر مهمة

| الأمر | الاستخدام |
|-------|-----------|
| `node backup-firestore.js` | نسخ احتياطي لقاعدة البيانات |
| `node restore-firestore.js backups/firestore-YYYY-MM-DD` | استعادة قاعدة البيانات |
| `npm install` | تثبيت الحزم بعد استعادة الكود |
| `npm run dev` | تشغيل التطبيق |

---

## تذكّر! ⚠️

- ✅ اعمل نسخة احتياطية **قبل** أي تعديل كبير
- ✅ احفظ النسخ في **3 أماكن** مختلفة
- ✅ احتفظ بـ **3 نسخ** على الأقل (جديدة، أسبوع، شهر)
- ✅ اختبر النسخة الاحتياطية **أحياناً** للتأكد من سلامتها

---

📖 **للتفاصيل الكاملة:** راجع ملف `دليل_النسخ_الاحتياطي_الشامل.md`
