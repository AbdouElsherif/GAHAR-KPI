# دليل مصور - النسخ الاحتياطي خطوة بخطوة 🖼️

## نظرة عامة على عملية النسخ الاحتياطي

```mermaid
flowchart TD
    A[🎯 البداية] --> B[📦 نسخ الكود]
    B --> C[💾 نسخ قاعدة البيانات]
    C --> D[☁️ حفظ في أماكن متعددة]
    D --> E[✅ النسخة الاحتياطية جاهزة!]
    
    style A fill:#4CAF50,color:#fff
    style E fill:#2196F3,color:#fff
```

---

## المرحلة الأولى: نسخ الكود البرمجي

### الخطوة 1: فتح المجلد

```mermaid
graph LR
    A[ابدأ] --> B[افتح File Explorer]
    B --> C["اذهب إلى d:\تطبيقي"]
    C --> D[شاهد كل الملفات]
    
    style A fill:#FF9800,color:#fff
```

**ما ستراه:**
- مجلد `app` 📁
- مجلد `components` 📁
- مجلد `lib` 📁
- ملف `.env.local` 📄
- ملف `package.json` 📄
- العديد من الملفات والمجلدات الأخرى

---

### الخطوة 2: إنشاء مجلد النسخ الاحتياطية

```mermaid
graph TD
    A[اذهب لسطح المكتب أو قرص D] --> B[انقر بالزر الأيمن]
    B --> C[اختر New ثم Folder]
    C --> D["اكتب: نسخ_احتياطية_التطبيق"]
    D --> E[افتح المجلد]
    E --> F["أنشئ مجلد جديد: نسخة_2025-12-12"]
```

**النتيجة المتوقعة:**
```
📁 نسخ_احتياطية_التطبيق/
   └── 📁 نسخة_2025-12-12/
       └── (فارغ حالياً)
```

---

### الخطوة 3: نسخ الملفات

```mermaid
sequenceDiagram
    participant U as أنت
    participant S as المجلد الأصلي
    participant D as مجلد النسخة الاحتياطية
    
    U->>S: افتح d:\تطبيقي
    U->>S: اضغط Ctrl + A
    Note over S: تم تحديد كل الملفات
    U->>S: اضغط Ctrl + C
    Note over S: تم نسخ الملفات
    U->>D: افتح مجلد النسخة الاحتياطية
    U->>D: اضغط Ctrl + V
    Note over D: جاري اللصق...
    D-->>U: ✅ تم النسخ بنجاح!
```

**مدة العملية:** 5-15 دقيقة (حسب حجم التطبيق)

---

## المرحلة الثانية: نسخ قاعدة البيانات

### العملية الكاملة

```mermaid
flowchart TD
    A[افتح VS Code] --> B[افتح Terminal]
    B --> C["تأكد من المسار: pwd"]
    C --> D{"هل أنت في d:\تطبيقي؟"}
    D -->|لا| E["اكتب: cd d:\تطبيقي"]
    D -->|نعم| F[اكتب: node backup-firestore.js]
    E --> F
    F --> G[انتظر حتى تنتهي العملية]
    G --> H{هل ظهرت All done!؟}
    H -->|نعم| I[✅ نجح النسخ الاحتياطي!]
    H -->|لا| J[❌ راجع الأخطاء]
    
    style I fill:#4CAF50,color:#fff
    style J fill:#F44336,color:#fff
```

---

### ما يحدث داخل الأمر `node backup-firestore.js`

```mermaid
sequenceDiagram
    participant U as أنت
    participant S as السكريبت
    participant F as Firestore
    participant D as ملفات JSON
    
    U->>S: node backup-firestore.js
    S->>F: طلب بيانات users
    F-->>S: إرسال بيانات المستخدمين
    S->>D: حفظ في users.json
    
    S->>F: طلب بيانات kpi_data
    F-->>S: إرسال بيانات التقارير
    S->>D: حفظ في kpi_data.json
    
    Note over S,D: تكرار للمجموعات الأخرى...
    
    S-->>U: ✅ Backup completed!
```

---

## المرحلة الثالثة: حفظ النسخ في أماكن متعددة

### قاعدة 3-2-1 المصورة

```mermaid
graph TD
    A[النسخة الأصلية] --> B[📁 نسخة 1: على جهازك]
    A --> C[💾 نسخة 2: هارد خارجي]
    A --> D[☁️ نسخة 3: السحابة]
    
    B --> E[وسيط 1: القرص الصلب]
    C --> F[وسيط 2: USB/HDD]
    
    D --> G[مكان خارجي]
    
    style A fill:#9C27B0,color:#fff
    style B fill:#2196F3,color:#fff
    style C fill:#FF9800,color:#fff
    style D fill:#4CAF50,color:#fff
```

**التوزيع المثالي:**
- ✅ **نسخة على جهازك** → للوصول السريع
- ✅ **نسخة على هارد خارجي** → للحماية من عطل الجهاز
- ✅ **نسخة على السحابة** → للحماية من الكوارث الطبيعية

---

## عملية الاستعادة الكاملة

```mermaid
flowchart TD
    A[🚨 مشكلة في التطبيق!] --> B{ما نوع المشكلة؟}
    
    B -->|مشكلة في الكود| C[السيناريو 1: استعادة الكود فقط]
    B -->|مشكلة في البيانات| D[السيناريو 2: استعادة البيانات فقط]
    B -->|انهيار كامل| E[السيناريو 3: استعادة كاملة]
    
    C --> F[نسخ الملفات من النسخة الاحتياطية]
    F --> G[تشغيل npm install]
    G --> H[تشغيل npm run dev]
    
    D --> I[تشغيل restore-firestore.js]
    I --> J[التحقق من البيانات]
    
    E --> K[استعادة الكود أولاً]
    K --> L[ثم استعادة البيانات]
    L --> M[التحقق من كل شيء]
    
    H --> N[✅ التطبيق يعمل!]
    J --> N
    M --> N
    
    style A fill:#F44336,color:#fff
    style N fill:#4CAF50,color:#fff
```

---

## استعادة الكود - خطوة بخطوة

```mermaid
stateDiagram-v2
    [*] --> OpenBackup: افتح النسخة الاحتياطية
    OpenBackup --> SelectAll: Ctrl + A
    SelectAll --> Copy: Ctrl + C
    Copy --> OpenOriginal: افتح d:\تطبيقي
    OpenOriginal --> Paste: Ctrl + V
    Paste --> WaitCopy: انتظر...
    WaitCopy --> RunNPM: npm install
    RunNPM --> WaitInstall: انتظر 5-10 دقائق
    WaitInstall --> RunDev: npm run dev
    RunDev --> [*]: ✅ جاهز!
```

---

## استعادة قاعدة البيانات - خطوة بخطوة

```mermaid
stateDiagram-v2
    [*] --> CheckBackupDir: تحقق من مجلد backups
    CheckBackupDir --> ChooseDate: اختر التاريخ المطلوب
    ChooseDate --> OpenTerminal: افتح Terminal
    OpenTerminal --> ChangeDir: cd d:\تطبيقي
    ChangeDir --> RunRestore: node restore-firestore.js backups/firestore-YYYY-MM-DD
    RunRestore --> WaitRestore: انتظر حتى All done!
    WaitRestore --> VerifyData: افتح التطبيق وتحقق
    VerifyData --> [*]: ✅ البيانات استُعيدت!
```

---

## جدول المقارنة: متى تستخدم أي سيناريو؟

| المشكلة | السيناريو المناسب | الوقت المتوقع | خطر فقدان البيانات |
|---------|-------------------|---------------|-------------------|
| ملف كود تالف | السيناريو 1 (استعادة الكود) | 15-20 دقيقة | ❌ لا يوجد |
| بيانات محذوفة خطأً | السيناريو 2 (استعادة البيانات) | 5-10 دقائق | ⚠️ بيانات بعد النسخة |
| التطبيق لا يعمل نهائياً | السيناريو 3 (استعادة كاملة) | 25-30 دقيقة | ⚠️ بيانات بعد النسخة |
| تحديث فاشل | السيناريو 1 أو 3 | 15-30 دقيقة | ⚠️ حسب الحالة |
| خطأ في قاعدة البيانات | السيناريو 2 | 5-10 دقائق | ⚠️ بيانات بعد النسخة |

---

## الخط الزمني: عمل نسخة احتياطية كاملة

```mermaid
gantt
    title الوقت المتوقع لكل خطوة
    dateFormat mm:ss
    axisFormat %M:%S
    
    section نسخ الكود
    فتح المجلدات           :00:00, 01:00
    تحديد ونسخ الملفات     :01:00, 00:30
    لصق الملفات            :01:30, 10:00
    
    section نسخ قاعدة البيانات
    فتح Terminal          :11:30, 00:30
    تشغيل السكريبت        :12:00, 00:30
    انتظار النسخ          :12:30, 03:00
    
    section الحفظ الخارجي
    نسخ للهارد الخارجي    :15:30, 05:00
    رفع للسحابة           :20:30, 10:00
```

**المجموع الكلي:** حوالي 30 دقيقة

---

## شجرة الملفات المتوقعة بعد النسخ الاحتياطي

```
📁 نسخ_احتياطية_التطبيق/
│
├── 📁 نسخة_2025-12-12/
│   ├── 📁 app/
│   ├── 📁 components/
│   ├── 📁 lib/
│   ├── 📁 node_modules/
│   ├── 📁 backups/
│   │   └── 📁 firestore-2025-12-12/
│   │       ├── 📄 users.json
│   │       ├── 📄 kpi_data.json
│   │       ├── 📄 moh_kpis.json
│   │       └── 📄 ...
│   ├── 📄 .env.local
│   ├── 📄 package.json
│   └── 📄 ...
│
├── 📁 نسخة_2025-12-05/
│   └── ...
│
└── 📁 نسخة_2025-11-28/
    └── ...
```

---

## الأخطاء الشائعة وحلولها

```mermaid
graph TD
    A{نوع الخطأ} --> B[Cannot find module]
    A --> C[Missing credentials]
    A --> D[Permission denied]
    A --> E[Backup not found]
    
    B --> B1[الحل: npm install firebase-admin]
    C --> C1[الحل: تحقق من ملف .env.local]
    D --> D1[الحل: شغّل VS Code كمسؤول]
    E --> E1[الحل: تحقق من اسم المجلد]
    
    style A fill:#FF9800,color:#fff
    style B1 fill:#4CAF50,color:#fff
    style C1 fill:#4CAF50,color:#fff
    style D1 fill:#4CAF50,color:#fff
    style E1 fill:#4CAF50,color:#fff
```

---

## نصائح الأمان

```mermaid
mindmap
  root((النسخ الاحتياطي الآمن))
    التكرار
      يومي للبيانات المهمة
      أسبوعي للاستخدام العادي
      شهري كحد أدنى
    التنوع
      أماكن مختلفة
      وسائط مختلفة
      نسخ متعددة
    الاختبار
      اختبر استعادة النسخة
      كل 3 أشهر
      قبل الحوادث
    التوثيق
      سجل التواريخ
      اكتب الملاحظات
      احفظ التفاصيل
```

---

## ملخص سريع مصور

```mermaid
flowchart LR
    A[🎯 ابدأ] --> B[📦 انسخ الكود<br/>Ctrl+A, Ctrl+C, Ctrl+V]
    B --> C[💾 انسخ البيانات<br/>node backup-firestore.js]
    C --> D[💿 احفظ في هارد خارجي]
    D --> E[☁️ ارفع للسحابة]
    E --> F[✅ انتهيت!<br/>لديك 3 نسخ آمنة]
    
    style A fill:#9C27B0,color:#fff
    style F fill:#4CAF50,color:#fff
```

---

## قائمة تحقق بصرية

```mermaid
graph TD
    A[☐ نسخت الكود] --> B[☐ نسخت قاعدة البيانات]
    B --> C[☐ نسخت للهارد الخارجي]
    C --> D[☐ رفعت للسحابة]
    D --> E[☐ اختبرت النسخة]
    E --> F[✅ جاهز للطوارئ!]
    
    style F fill:#4CAF50,color:#fff
```

---

> [!NOTE]
> هذا الدليل المصور يكمّل الدليل النصي الشامل. استخدمهما معاً لفهم أفضل!

---

*آخر تحديث: 2025-12-12*
*الرسوم التوضيحية تُعرض تلقائياً في VS Code* 🎨
