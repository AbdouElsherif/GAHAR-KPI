# 📦 دليل النسخ الاحتياطي والاستعادة
# GAHAR KPI Backup & Restore Guide

---

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية عمل نسخة احتياطية كاملة من تطبيق GAHAR KPI واستعادته في حالة الحاجة.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر:

- ✅ PowerShell 5.0 أو أحدث
- ✅ Node.js و npm مُثبتين
- ✅ Firebase CLI (اختياري للنسخ الاحتياطي المتقدم)
- ✅ مساحة تخزين كافية (على الأقل 500 MB)

---

## 🔄 عمل نسخة احتياطية

### الطريقة السريعة (موصى بها)

```powershell
# افتح PowerShell في مجلد المشروع
cd D:\تطبيقي

# شغل سكريبت النسخ الاحتياطي
.\scripts\backup.ps1
```

الملف سيُحفظ تلقائياً في: `D:\Backups\GAHAR-KPI\`

---

### خيارات متقدمة

#### 1. تغيير موقع الحفظ

```powershell
.\scripts\backup.ps1 -BackupPath "E:\MyBackups"
```

#### 2. تخطي نسخ Firebase (أسرع)

```powershell
.\scripts\backup.ps1 -SkipFirebase
```

#### 3. نسخة كاملة مع تحديد المسار

```powershell
.\scripts\backup.ps1 -BackupPath "E:\Backups\GAHAR" -SkipFirebase:$false
```

---

## 📦 محتويات النسخة الاحتياطية

كل نسخة احتياطية تحتوي على:

### 1. **الكود البرمجي** (`code/`)
- جميع ملفات التطبيق
- Components, pages, styles
- صورة كاملة من الكود

### 2. **المتغيرات البيئية** (`environment/`)
- `.env.local` - إعدادات Firebase
- جميع المفاتيح السرية

### 3. **بيانات Firebase** (`firebase-data/`)
- `kpi-data.json` - جميع بيانات KPI
- `users.json` - المستخدمين والصلاحيات
- `backup-metadata.json` - معلومات النسخة

### 4. **ملف المعلومات**
- `BACKUP-INFO.md` - تفاصيل النسخة وتعليمات الاستعادة

---

## 🔧 استعادة النسخة الاحتياطية

### الاستعادة الكاملة

```powershell
# استعادة إلى موقع جديد
.\scripts\restore.ps1 -BackupZipPath "D:\Backups\GAHAR-KPI\GAHAR-KPI-Backup_2025-11-26_09-00-00.zip"
```

سيتم:
1. ✅ فك ضغط النسخة
2. ✅ استعادة الكود
3. ✅ نسخ ملفات .env
4. ✅ تثبيت npm packages
5. ✅ تجهيز المشروع للتشغيل

---

### استعادة مع بيانات Firebase

```powershell
.\scripts\restore.ps1 -BackupZipPath "PATH\TO\BACKUP.zip" -RestoreFirebase
```

⚠️ **ملاحظة:** استعادة Firebase تتطلب خطوات يدوية إضافية (انظر القسم التالي)

---

## 🔥 استعادة بيانات Firebase يدوياً

### الخطوة 1: تثبيت Firebase CLI

```powershell
npm install -g firebase-tools
firebase login
```

### الخطوة 2: استعادة Firestore

```powershell
# من مجلد المشروع المُستعاد
cd D:\تطبيقي-restored\code

# تشغيل سكريبت الاستعادة المخصص
node scripts/firebase-restore.js ./firebase-restore-data
```

### الخطوة 3: استعادة المستخدمين

```powershell
firebase auth:import ./firebase-restore-data/users.json --project YOUR-PROJECT-ID
```

---

## ⏰ جدولة النسخ الاحتياطي التلقائي

### إنشاء مهمة مجدولة (Task Scheduler)

1. افتح **Task Scheduler** (مجدول المهام)
2. **Create Basic Task**
3. اختر التوقيت (يومي/أسبوعي/شهري)
4. Action: **Start a Program**
   - Program: `powershell.exe`
   - Arguments: `-File "D:\تطبيقي\scripts\backup.ps1"`
5. Finish

**التوقيت الموصى به:** أسبوعياً (كل يوم جمعة الساعة 2 صباحاً)

---

## 📊 مقارنة الطرق

| الطريقة | السرعة | الحجم | الاكتمال |
|---------|--------|-------|----------|
| نسخة كاملة | 🐢 بطيئة | 📦 كبير | ✅ 100% |
| بدون Firebase | 🚀 سريعة | 📦 صغير | ⚠️ 70% |
| كود فقط | ⚡ فورية | 📦 صغير جداً | ⚠️ 40% |

---

## 🔐 الأمان وأفضل الممارسات

### ✅ افعل:

- ✅ احفظ النسخ في مكان آمن ومُشفر
- ✅ قم بنسخ احتياطي منتظم (أسبوعياً على الأقل)
- ✅ احتفظ بـ 3 نسخ على الأقل (الأخيرة + 2 قديمة)
- ✅ اختبر الاستعادة بشكل دوري

### ❌ لا تفعل:

- ❌ لا تشارك ملفات .env مع أحد
- ❌ لا ترفع النسخ على GitHub/Cloud عام
- ❌ لا تعتمد على نسخة واحدة فقط
- ❌ لا تهمل حذف النسخ القديمة جداً

---

## 🆘 استكشاف الأخطاء

### المشكلة: "Cannot find module 'firebase-admin'"

**الحل:**
```powershell
npm install firebase-admin --save-dev
```

### المشكلة: "Access Denied" عند النسخ

**الحل:**
```powershell
# شغل PowerShell كـ Administrator
```

### المشكلة: ملف ZIP تالف

**الحل:**
- تأكد من اكتمال عملية النسخ الاحتياطي
- تحقق من مساحة التخزين المتاحة
- جرب تشغيل السكريبت مرة أخرى

---

## 📞 الدعم

في حالة مواجهة مشاكل:

1. راجع هذا الدليل بالكامل
2. تحقق من logs في PowerShell
3. تأكد من صحة ملفات .env
4. اتصل بالدعم الفني

---

## 📝 ملاحظات إضافية

### حجم النسخة المتوقع

- **كود فقط:** ~5-10 MB
- **كود + env:** ~5-10 MB
- **كاملة مع Firebase:** ~20-50 MB (حسب حجم البيانات)

### مدة النسخ

- **سريعة (بدون Firebase):** 10-30 ثانية
- **كاملة:** 1-3 دقائق

### مدة الاستعادة

- **فك الضغط:** 10 ثانية
- **npm install:** 1-3 دقائق
- **الإجمالي:** 2-5 دقائق

---

## 🎓 خلاصة سريعة

```powershell
# عمل نسخة احتياطية
.\scripts\backup.ps1

# استعادة النسخة
.\scripts\restore.ps1 -BackupZipPath "PATH\TO\BACKUP.zip"

# تشغيل المشروع المُستعاد
cd D:\تطبيقي-restored\code
npm run dev
```

---

**تم إنشاء هذا الدليل بواسطة: GAHAR Backup System v1.0**  
**آخر تحديث:** 2025-11-26
